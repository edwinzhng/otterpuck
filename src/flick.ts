// A flick launches the puck at FLICK_BASE + FLICK_GAIN * power meters per
// second before the swimmer's own speed and strength are applied.
export const FLICK_BASE = 2.2;
export const FLICK_GAIN = 2.8;
